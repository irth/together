<p class="text-2xl">
  <% if !logged_in? %>
  Compare your music tastes!</p>
  <% else %>
  Hi, <span class="text-green-light"><%= current_user.display_name %>.</span>
  <% end %>
</p>
<p class="text-xl mt-4">
  Find songs that both you and your friend saved and listen to them
  together.
</p>
<% if !logged_in? %>
<p>
  <%= link_to 'Log in via Spotify', auth_path(:spotify), class: "big-round-button" %>
</p>
<p>
  By logging in you consent to the <b>terms of service</b> and the <b>privacy policy</b>.
</p>
<% else %>
<p>
  <%= link_to 'Create a playlist', new_playlist_path, class: 'big-round-button', method: :post %>
</p>
<% end %>